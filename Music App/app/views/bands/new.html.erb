<h1>Adding New Band</h1>

<form action="/bands" method="POST">
  <label for="band_name">Name</label>
  <input type="text"
         name="band[name]"
         id="band_name"
         value="<%= @band.name %>">

  <input type="submit" value="Add band!">
</form>
